{
  "name": "malamamapdb",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "sync-ui": "node scripts/sync-ui.mjs",
    "predeploy": "npm run sync-ui",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^26.1.1",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.113.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.8",
    "hono": "^4.12.31"
  }
}
